const config = () => {
  let environment = {
    'environment': 'localhost:5000',
    'registration-url': 'localhost:5000/user/register-form-show',
  };
 return environment;
};

module.exports = config();
